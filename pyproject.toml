[tool.poetry]
name = "runtime_preproc"
version = "0.1.0"
description = ""
authors = ["Andrew Lee <candrewlee14@gmail.com>"]
readme = "README.md"
packages = [{include = "runtime_preproc"}]

[tool.poetry.dependencies]
python = "^3.10"
tree-sitter = "^0.20.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
